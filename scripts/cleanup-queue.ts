#!/usr/bin/env tsx

// This script cleans up the combat queue for testing purposes
// Run with: npx tsx scripts/cleanup-queue.ts

console.log('⚠️  This script needs to be run with proper authentication.');
console.log('For now, you can manually clear the queue by:');
console.log('1. Going to the arena page');
console.log('2. Using the Leave Queue button if your gladiator is in queue');
console.log('');
console.log('Or use Supabase dashboard to manually delete entries from combat_queue table.');
